# Shared action: build application (DRY — single place for build step)
name: Build app
description: Build the application (placeholder; replace with real build command)
runs:
  using: composite
  steps:
    - name: Build app (placeholder)
      shell: bash
      run: |
        echo "Building app (placeholder)"
        echo "TODO: replace this step with real build commands."
