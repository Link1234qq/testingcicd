# Composite action: generic test placeholder
name: Run tests
description: Placeholder — echo only, no real tests
runs:
  using: composite
  steps:
    - name: Run tests (placeholder)
      shell: bash
      run: |
        echo "[MOCK] Running tests (placeholder)"
