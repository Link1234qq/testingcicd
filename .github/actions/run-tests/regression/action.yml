# Composite action: regression tests (placeholder)
name: Regression tests
description: Run regression tests
runs:
  using: composite
  steps:
    - name: Regression tests (placeholder)
      shell: bash
      run: |
        echo "[MOCK] Regression testing (placeholder)"
